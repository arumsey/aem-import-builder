  Problem Statement:
    1. You are a developer that is tasked to migrate an existing website to the Adobe Edge Delivery Services platform.
    2. Given the following html: {{{content}}}, determine which elements represent {{pattern}}.
    3. Provide a single CSS selector string for each of the elements that were found.
  CSS Selector Requirements:
    1. If multiple elements are children of each other only use the CSS selector that represents the top-most parent.
    2. Do not duplicate CSS selectors.
    3. Respond in the format {selectors: [...]}

